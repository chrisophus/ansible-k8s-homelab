# Vagrant-specific overrides
# Kubernetes version
kube_version: "1.30.0-1.1"
kube_version_short: "v1.30.0"

# Container runtime
containerd_version: "1.7.13-1"

# Keepalived VIP configuration for Vagrant
keepalived_interface: "enp0s8"         # Vagrant's second interface
keepalived_vip: "192.168.56.100"       # VIP in Vagrant network
keepalived_auth_pass: "vagrant123!"    # Test password
keepalived_priority: 100               # Default priority