# Kubernetes version
kube_version: "1.31.0-1.1"
kube_version_short: "v1.31.0"

# Container runtime
containerd_version: "1.7.13-1"

# Keepalived VIP configuration
keepalived_interface: "enp3s0"        # Ethernet interface found on all nodes
keepalived_vip: "192.168.0.100"       # VIP in your network range
keepalived_auth_pass: "changeme123!"  # Change this in production
keepalived_priority: 100             # Will be overridden per host

# Monitoring configuration
grafana_admin_password: "homelab123!"
alert_email_to: "admin@homelab.local"
alert_email_from: "alerts@homelab.local"
smtp_server: "localhost:587"